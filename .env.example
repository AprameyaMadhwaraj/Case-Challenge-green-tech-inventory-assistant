# Green-Tech Inventory Assistant — environment variables
# Copy this file to .env and fill in values. Do not commit .env or real API keys.

# Optional: Gemini API key for AI-powered reorder insights.
# If unset, the app uses a rule-based forecast (average daily consumption) only.
GEMINI_API_KEY=your_gemini_api_key